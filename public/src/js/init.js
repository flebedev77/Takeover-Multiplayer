const socket = io();

window.addEventListener("load", function() {
    resizeCanvasToWindow();
});
window.addEventListener("resize", function() {
    resizeCanvasToWindow();
});

function init() {
    
}